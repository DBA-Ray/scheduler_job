---
# environment variable
business_env: shanghai_pro
# primary hostname for mgr
primary_hostname: mgr1:mgr4
# defaults file for scheduler_job
install_dir: /var/local
basedir: "{{ install_dir }}/mysql"
shelldir: "{{ install_dir }}/shell"
monitordir: "{{ install_dir }}/monitor"
# Xtrabackup info
xtrabackup_full: "{{ ansible_hostname }}_xxx_full"
backupdir: "{{ install_dir }}/backup"
datadir: "{{ basedir }}/data"
etcdir: "{{ basedir }}/etc"
logdir: "{{ basedir }}/log"
# Mysqldump info
dbname: dxz
binlog_index: "{{ datadir }}/mysql-bin.index"
restore_host: 10.0.64.6
restore_host_port: 58241
set_gtid_purged: 'off'
# Mysqlrestore info
restore_diff_database: True
restore_target_data_name: dxz414.sql
restore_target_data: "/tmp/{{ restore_target_data_name }}"
restore_target_database: test
# Mysql connection info
mysql_port: 3377
socket: /tmp/mysql.sock
# General mail info
scp_port: 59410
send_host: 172.21.0.38
recipient: rulei@jjl.cn
# Archive tables info
archive_dic: >
             "[dxz_user_resource_access]=\"created_at < '2019-7-5 00:00:00'\"
              [dxz_user_resource_access_copy1]=\"created_at < '2019-6-30 00:00:00'\""
source_info: "localhost 3377 dxz"
dest_info: "localhost 3377 dxz"
arc_suffix: arc