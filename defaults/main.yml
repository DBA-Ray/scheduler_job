---
# defaults file for scheduler_job
install_dir: /var/local
basedir: "{{ install_dir }}/mysql"
shelldir: "{{ install_dir }}/shell"
monitordir: "{{ install_dir }}/monitor"
# Archived backup base path
xtrabackup_full: "{{ ansible_hostname }}_xxx_full"
backupdir: "{{ install_dir }}/backup"
datadir: "{{ basedir }}/data"
etcdir: "{{ basedir }}/etc"
logdir: "{{ basedir }}/log"
socket: /tmp/mysql.sock
dbname: dxz
binlog_index: "{{ datadir }}/mysql-bin.index"
mysql_port: 3377
scp_port: 59410
send_host: 172.21.0.38
recipient: rulei@jjl.cn
#,zhangzhenjun@jjl.cn
# archive tables info
archive_dic: "[dxz_user_resource_access]=\"created_at < '2019-7-5 00:00:00'\" [dxz_user_resource_access_copy1]=\"created_at < '2019-6-30 00:00:00'\""
source_info: "localhost 3377 dxz"
dest_info: "localhost 3377 dxz"