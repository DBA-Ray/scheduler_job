- name: Create a directory for archiving mysql tables
  file: path={{ shelldir }} state=directory owner=mysql group=mysql
  when: ansible_hostname == "mgr1"

- name: Rendering archive mysql tables script file
  template:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    owner: mysql
    group: mysql
    mode: 0744
  with_items:
    - { src: 'mysql_archive_tables.sh.j2', dest: '{{ shelldir }}/mysql_archive_tables.sh' }
  when: ansible_hostname == "mgr1"